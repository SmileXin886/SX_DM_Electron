# Dreamina Toolkit - Python 中转端依赖
# Python 3.9+ 推荐

# Web 服务与 WebSocket 通信
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.9   # FastAPI 文件上传（multipart/form-data）

# 媒体处理 - 视频预览和缩略图生成
# 已废弃：缩略图和时长提取全部由前端（浏览器原生 Video/Audio/Image 解码）完成，无需后端依赖
# 暂时废弃
# opencv-python>=4.8.0  # 已废弃
# ffmpeg-python>=0.2.0  # 已废弃

# 浏览器控制 (CDP 接管)
playwright>=1.41.0

# 加密解密（AES-256-GCM .sxc 文件解密）
cryptography>=41.0.0

# HTTP 客户端 (用于可能的外部 API 调用)
httpx>=0.26.0
